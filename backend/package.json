{
  "name": "regresyon-otomasyonu",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "npx playwright test",
    "install-browsers": "npx playwright install",
    "build": "npm install && npx playwright install --with-deps chromium",
    "docker:build": "docker build -t otomasyon-backend .",
    "docker:run": "docker run -p 8080:8080 otomasyon-backend",
    "railway:deploy": "railway up"
  },
  "keywords": ["playwright", "automation", "testing"],
  "author": "",
  "license": "ISC",
  "description": "Playwright test automation backend service",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.1",
    "@types/node": "^24.0.7",
    "nodemon": "^3.0.1"
  }
}
